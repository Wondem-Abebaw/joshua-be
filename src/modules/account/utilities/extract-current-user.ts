export const extractCurrentUser = (user: any, headers: any) => {
  // console.log(user);
  // const { id, firstName, lastName, email, role } = user;
  // const userInfo: any = {
  //   id,
  //   email,
  //   firstName,
  //   lastName,
  //   fullName: `${firstName} ${lastName}`,
  //   role,
  // };

  return user;
};
